[wofi]
extra_args = "--allow-markup --columns=1 --hide-scroll"

[menu.shutdown]
title = "Shutdown"
requires_confirmation = "true"

[menu.reboot]
requires_confirmation = "true"

[menu.hibernate]
requires_confirmation = "true"

[menu.suspend]
requires_confirmation = "true"

[menu.logout]
requires_confirmation = "false"

[menu.lock-screen]
title = "Lock"
requires_confirmation = "true"
cmd = "hyprlock"

